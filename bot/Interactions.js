const Discord = require('discord.js');

exports.interactions =
{
    bond: function (db, msg)
    {
        //pool connect
        //query for specified hatchling
        //query for user lvl, exp
        //query for inventory glimmer, ether
    },

    bondAll: function (db, msg)
    {
        //call the bond function using for loop based on rows.length
    }
}